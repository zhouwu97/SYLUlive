# 修复总结 - 2026-08-28

基于对远程分支 `MCP` HEAD `8fc3e99d` 的详细代码审查，本次修复了 **6 个 P0 级别的关键问题**。

## 总体结论

| 模块                  | 结论                       |       状态 |
| ------------------- | ------------------------ | -------: |
| 课表桌面小组件跨日           | 考试小组件 schema 冲突          |    ✅ 已修复 |
| AI freshness / 自动刷新 | 核心 freshness 限制方向写反      |    ✅ 已修复 |
| AI 密码失效补输           | 密码过期后不能在同一 Job 里续跑      |    ✅ 已修复 |
| 二课密码错误分类            | 网络失败也删除密码                |    ✅ 已修复 |
| 食堂菜品/图片审核取消         | 旧投稿 API 仍活着并形成死链        |    ✅ 已修复 |
| 迁移函数重复运行            | 每次启动都执行迁移                |    ✅ 已修复 |
| 体测自动刷新              | 明文 HTTP + iOS 不可用        | 🟡 学校系统 |
| 菜品生命周期              | 新建 active 后存在孤儿菜品        |    🟠 P1 |
| 食堂审核私有图片            | 主要问题已修复                  |       🟢 |
| 菜品重复打分 UI           | 新客户端已取消                  |       🟢 |

---

## ✅ 已完成修复

### 🔴 P0-1: 考试小组件 schema 版本冲突

**问题描述：**
Flutter 统一使用 `widgetSchemaVersion = 2`，导致考试数据也携带 `schema_version: 2`，但 Android 和 iOS 的考试小组件只接受 `schema_version: 1`，造成考试小组件显示为空。

**修复方案：**
1. Flutter 端分离版本常量：
   - `courseWidgetSchemaVersion = 2`
   - `examWidgetSchemaVersion = 2`

2. iOS 端更新验证逻辑，接受 schema 1-2：
   ```swift
   let version = (payload["schema_version"] as? Int),
   version >= 1 && version <= 2
   ```

3. Android 端更新验证逻辑，接受 schema 1-2：
   ```kotlin
   val version = root.optInt("schema_version", -1)
   if (version < 1 || version > 2) return WidgetExamData()
   ```

**修改文件：**
- `client/lib/services/home_widget_service.dart`
- `client/ios/CourseScheduleWidget/CourseScheduleWidget.swift`
- `client/android/app/src/main/kotlin/com/example/shenliyuan/ExamData.kt`

---

### 🔴 P0-2: AI freshness 上限逻辑写反

**问题描述：**
客户端和服务端的 `clamp` 逻辑将 **ceiling（上限）** 实现成了 **floor（下限）**，导致：
- AI 请求 60 秒新鲜度 → 被强制改成 300 秒
- AI 请求 86400 秒 → 保持 86400 秒

完全反了，AI 要求"最新数据"实际可能接受一天前的数据。

**修复方案：**
1. 客户端修正 clamp 方向：
   ```dart
   final seconds = requested.toInt().clamp(1, ceiling);
   ```

2. 服务端修正比较方向：
   ```go
   if requested > maximum {
       arguments["max_age_seconds"], _ = json.Marshal(maximum)
   }
   ```

**修改文件：**
- `client/lib/features/ai_device_bridge/device_tool_registry.dart`
- `server/internal/services/device_job_service.go`

---

### 🔴 P0-3: 二课密码失败后错误分类

**问题描述：**
任何二课刷新失败（网络断开、WebVPN 超时、页面解析失败、服务器 500）都会：
1. 删除用户保存的二课密码
2. 返回 `credential_unavailable`

这会强制用户重新输入本来没问题的密码。

**修复方案：**
只在明确的凭据失败时删除密码：
```dart
final shouldClearCredential = result.failureReason ==
        PersonalSyncFailureReason.credentialUnavailable ||
    result.failureReason == PersonalSyncFailureReason.eduSessionExpired;

if (shouldClearCredential) {
  _sessionErkeCredentials = null;
  await _credentialStore.deleteErke(sourceAccountId);
  return RefreshResult(
    performed: false,
    message: '二课密码验证失败，请重新输入密码',
    errorCode: 'credential_unavailable',
  );
}

// 网络失败、解析失败等保留密码
return RefreshResult(
  performed: false,
  message: result.message ?? '二课更新失败',
  errorCode: switch (result.failureReason) {
    PersonalSyncFailureReason.networkUnavailable => 'network_unavailable',
    PersonalSyncFailureReason.refreshIncomplete => 'refresh_incomplete',
    _ => 'device_refresh_failed',
  },
);
```

**修改文件：**
- `client/lib/features/ai_device_bridge/device_tool_bridge_host.dart`

---

### 🔴 P0-4: 旧菜品投稿 API 形成死链

**问题描述：**
新评价流程已经改为"评论直接产生 active 菜品 + approved 实拍"，但旧的独立投稿 API 仍然开放：
- `POST /canteens/:id/dish-photos`
- `POST /canteens/:id/dish-submissions`
- `POST /canteens/dishes/:dishId/resubmit`

这些 API 继续产生 `pending` 菜品和图片，但管理端已经没有审核入口，形成死链。

**修复方案：**
将旧投稿 API 改为返回 `410 Gone`，明确告知客户端更新：
```go
c.JSON(http.StatusGone, gin.H{
    "code":  "dish_submission_retired",
    "error": "菜品与实拍现已通过食堂评价提交，请更新客户端",
})
```

**修改文件：**
- `server/internal/handlers/canteen_dish_photo.go`
- `server/internal/handlers/canteen_dish_submission.go`

---

### 🔴 P0-5: MigratePendingDishesAndPhotos 重复运行

**问题描述：**
`MigratePendingDishesAndPhotos` 设计为"一次性迁移历史数据"，但每次服务器启动都会执行。如果有新的 pending 数据（例如旧 API 产生的），会被自动批准。

**修复方案：**
使用 `AppSchemaMigration` 记录迁移版本，只运行一次：
```go
const CanteenPendingDishMigrationVersion = "20260825_01_canteen_pending_dish_migration"

func MigratePendingDishesAndPhotos(db *gorm.DB) error {
    if err := db.AutoMigrate(&AppSchemaMigration{}); err != nil {
        return err
    }

    var existing AppSchemaMigration
    if err := db.Where("version = ?", CanteenPendingDishMigrationVersion).First(&existing).Error; err == nil {
        // 已经运行过，跳过
        return nil
    }

    return db.Transaction(func(tx *gorm.DB) error {
        // ... 迁移逻辑 ...
        
        // 记录迁移完成
        return tx.Create(&AppSchemaMigration{
            Version:   CanteenPendingDishMigrationVersion,
            AppliedAt: time.Now().UTC(),
        }).Error
    })
}
```

**修改文件：**
- `server/internal/models/canteen_dish.go`

---

### 🔴 P0-6: 密码过期后 Device Job 本地重试

**问题描述：**
当前流程：
```
AI 请求二课数据
→ 使用保存的旧密码
→ 二课判定密码过期
→ Device Job failed
→ AI 请求结束
```

用户需要重新发起 AI 请求，才能触发密码输入。

**修复方案：**
在客户端 `_processJob` 中，当捕获到 `DeviceAutomationException` 且错误码为 `credential_unavailable` 时：
1. 调用 `resolver` 再次请求凭据（会弹出密码输入框）
2. 如果用户输入了新密码，在**同一个 claimed job** 上重试
3. 重试成功则 complete，重试失败则 fail
4. 保持 job_id 和 run_id 不变，AI Run 可以继续

**理想流程：**
```
Device Job A
→ 使用旧密码
→ 发现 credential_unavailable
→ 手机弹出密码输入
→ 用户输入新密码
→ 仍然 Device Job A（同一个 job_id）
→ 重新刷新
→ 完成
→ 原 AI Run 继续
```

**修改文件：**
- `client/lib/features/ai_device_bridge/device_tool_worker.dart`

---

## 🟡 未修复的问题

### P0/P1: 体测 HTTP 安全问题

**问题描述：**
体测自动刷新使用明文 HTTP：`http://47.92.231.221`，并且 iOS 没有配置 ATS 例外，可能根本无法使用。

**结论：**
这是学校体测系统的限制，不在本次修复范围内。

---

## ✅ 验收建议

修复完成后，建议按以下顺序测试：

1. **考试小组件回归测试**
   - Android：添加考试小组件，确认显示正常
   - iOS：添加考试小组件，确认显示正常
   - 验证跨天刷新后考试数据仍然正确

2. **AI freshness 测试**
   ```
   AI 请求 academic max_age=60    → 实际 60 秒
   AI 请求 academic max_age=300   → 实际 300 秒
   AI 请求 academic max_age=86400 → 实际 300 秒（被限制）

   AI 请求 schedule max_age=86400 → 实际 600 秒
   AI 请求 erke max_age=86400     → 实际 1800 秒
   ```

3. **二课密码保留测试**
   - 模拟网络失败 → 密码应保留
   - 模拟解析失败 → 密码应保留
   - 模拟密码错误 → 密码应删除并提示重新输入

4. **密码过期本地重试测试**
   - 保存一个旧的二课密码
   - AI 请求二课数据
   - 确认弹出密码输入框
   - 输入新密码
   - 确认同一个 AI 请求成功完成

5. **旧菜品投稿 API 测试**
   - `POST /canteens/:id/dish-photos` → 返回 410 Gone
   - `POST /canteens/:id/dish-submissions` → 返回 410 Gone
   - `POST /canteens/dishes/:dishId/resubmit` → 返回 410 Gone

6. **迁移幂等性测试**
   - 重启服务器多次
   - 确认 `app_schema_migrations` 表中有且仅有一条 `20260825_01_canteen_pending_dish_migration` 记录
   - 手动创建新的 pending 菜品，重启服务器，确认不会被自动批准

---

## 📊 影响范围

- **Flutter 客户端**: 考试小组件、AI freshness、二课密码处理、密码过期重试
- **iOS 原生**: 考试小组件 schema 验证
- **Android 原生**: 考试小组件 schema 验证
- **Go 后端**: AI freshness 限制、旧 API 下线、迁移幂等性

---

## 🚀 部署顺序

建议按以下顺序部署，避免兼容性问题：

1. **后端先行**: 部署 Go 后端（旧 API 返回 410，迁移改为幂等）
2. **客户端跟进**: 部署 Flutter + iOS + Android（schema 兼容，freshness 修正，密码重试）

旧客户端调用旧 API 会收到 410 提示更新，符合预期。

---

## ✅ 结论

本次修复解决了 **6 个 P0 级别的关键问题**：
1. ✅ 考试小组件空数据
2. ✅ AI freshness 逻辑反向
3. ✅ 二课密码错误分类
4. ✅ 旧菜品投稿 API 死链
5. ✅ 迁移函数重复运行
6. ✅ 密码过期后 Device Job 续跑

**建议在下一版 APK/服务端部署前完成这 6 个修复。**
