abstract final class EmojiPackLimits {
  static const maxArchiveBytes = 100 * 1024 * 1024;
  static const maxExpandedBytes = 300 * 1024 * 1024;
  static const maxAssetCount = 500;
  static const maxSingleAssetBytes = 15 * 1024 * 1024;
  static const maxManifestBytes = 1024 * 1024;
  static const maxStaticDimension = 4096;
  static const maxAnimatedDimension = 2048;
  static const maxGifFrames = 500;
  static const maxDecodedPixels = 100 * 1024 * 1024;
}
