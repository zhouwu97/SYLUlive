// Compatibility re-export: the real implementation lives in the publish
// sub-directory so all existing call sites keep working without import-path
// changes.
export 'publish/create_post_screen.dart';
