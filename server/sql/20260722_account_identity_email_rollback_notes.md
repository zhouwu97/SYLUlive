# 账号身份迁移回退说明

正式迁移会把历史 QQ 账号的 `student_id` 转为空字符串或已认证学号，并清空 Go 侧历史教务凭据。该操作不可通过 SQL 安全地无损自动回退。

回退流程：停止服务，恢复迁移前 PostgreSQL 备份；不要尝试依据日志、邮箱或 Python 凭据自动重建旧账号字段。若仅需撤回应用版本，可保持新 schema 并部署兼容版本，旧客户端仍可通过 `student_id` 和 `edu_bound` 工作。
